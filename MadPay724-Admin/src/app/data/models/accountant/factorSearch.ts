export interface FactorSearch {
    status: number,
    factorType: number,
    minPrice: number,
    maxPrice: number,
    minDate: number,
    maxDate: number,
    bank: number,
    filter: string
}
